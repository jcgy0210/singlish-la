import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SinglishLA - Courses',
};

export default async function Page() {
    return(
        <p>This is Courses Page.</p>
    )
}